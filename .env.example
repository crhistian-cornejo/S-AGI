# =================================================================
# S-AGI Environment Variables (Example)
# =================================================================

# --- OBLIGATORIAS (predev las valida) ---
# Supabase: Project Settings > API en https://supabase.com/dashboard
VITE_SUPABASE_URL=your_supabase_project_url
VITE_SUPABASE_ANON_KEY=your_supabase_anon_key
MAIN_VITE_SUPABASE_URL=your_supabase_project_url
MAIN_VITE_SUPABASE_ANON_KEY=your_supabase_anon_key

# --- OAuth / deep linking ---
# Protocolo para callbacks OAuth (Claude, etc.). Dejar así para s-agi://auth/callback
VITE_OAUTH_REDIRECT_URI=s-agi://auth/callback

# --- AI (opcional) ---
# Claude: OAuth en la app (Settings > API Keys > Connect Claude Code). No env.
# OpenAI: solo si usas modelos OpenAI/ChatGPT
OPENAI_API_KEY=
VITE_OPENAI_API_KEY=

# --- Opcionales ---
SUPABASE_ACCESS_TOKEN=

# macOS signing (solo para build/release)
APPLE_IDENTITY=
APPLE_TEAM_ID=
APPLE_ID=
APPLE_APP_SPECIFIC_PASSWORD=
